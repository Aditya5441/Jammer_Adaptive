# Jammer_Adaptive
an ESP32-based WiFi/BLE jamming device that uses chaos-derived entropy (from internal noise/analog readings) to drive non-deterministic frequency hopping, timing, and payload generation instead of static, predictable patterns. It includes a self-mutating encryption layer that regenerates data each cycle
